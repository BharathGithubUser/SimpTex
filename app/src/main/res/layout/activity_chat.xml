<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".modules.userslist.ChatUsersActivity">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_chats"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/ll_send_message" />

    <RelativeLayout
        android:id="@+id/ll_send_message"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_alignParentRight="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentBottom="true"
        android:background="#A1F3EFEF"
        android:layout_alignParentEnd="true"
        android:layout_alignParentStart="true">

        <EditText
            android:id="@+id/et_msg_to_send"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_alignParentBottom="true"
            android:layout_toStartOf="@+id/btn_send_msg"
            android:layout_toLeftOf="@+id/btn_send_msg"
            android:hint="Enter Your Message Here!"
            android:padding="2dp"
            android:textSize="20sp" />

        <Button
            android:id="@+id/btn_send_msg"
            android:layout_width="75dp"
            android:layout_height="match_parent"
            android:layout_alignTop="@+id/et_msg_to_send"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:layout_gravity="center"
            android:background="@drawable/ic_send_msg"
            android:textAlignment="center" />
    </RelativeLayout>

</RelativeLayout>